# updev v0.7.2

`updev v0.7.2` is a public-preview maintenance patch for the v0.7 support-label
contract tests.

## Highlights

- Strengthens the compatibility-ledger JSON regression test so it verifies the
  `support_label` key exists inside each ledger entry object.

## Upgrade Notes

No config migration is required.

## Validation Highlights

- `mise -C tools/updev run check`
- `mise -C tools/updev run docs-check`
- `git diff --check`
- `chezmoi apply --dry-run`
- public export smoke:
  - `scripts/check-docs.sh`
  - `go test ./...`
  - `go vet ./...`
  - `go mod verify`
  - `go build ./...`
