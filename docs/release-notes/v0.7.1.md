# updev v0.7.1

`updev v0.7.1` is a public-preview maintenance patch for the v0.7 support-label
contract.

## Highlights

- Makes compatibility-ledger `support_label` an unconditional JSON field.

## Upgrade Notes

No config migration is required.

## Validation Highlights

- `mise -C tools/updev run check`
- `mise -C tools/updev run docs-check`
- `git diff --check`
- `chezmoi apply --dry-run`
- public export smoke:
  - `scripts/check-docs.sh`
  - `go test ./...`
  - `go vet ./...`
  - `go mod verify`
  - `go build ./...`
