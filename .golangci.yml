version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  default: none
  enable:
    - bodyclose
    - govet
    - misspell
    - noctx
    - nolintlint
    - tparallel
    - whitespace

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
